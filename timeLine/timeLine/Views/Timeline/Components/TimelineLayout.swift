import SwiftUI
import Combine

// MARK: - Layout Constants
enum TimelineLayout {
    // Using fixed inset to avoid deprecated UIScreen.main
    static let horizontalInset: CGFloat = 24
    static let rowCornerRadius: CGFloat = 12
    static let defaultRowHeight: CGFloat = 76
}
